spring:
  profiles:
    active: dev

---

spring:
  profiles: dev
  application:
    name: company-management-service
  cloud:
    config:
      uri:
      username:
      password:

---

spring:
  profiles: prod
  application:
    name: company-management-service
  cloud:
    config:
      uri:
      username:
      password: